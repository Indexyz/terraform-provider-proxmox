# Roadmap

## 已完成

- 扫描项目结构、Provider 配置、HTTP client、资源、数据源、QEMU VM typed/raw 映射、文档生成和 CI/e2e 工具链。
- 新增 `docs/codebase.md`，整理开发者向代码库说明、API surface、资源/数据源职责、QEMU 扩展边界、测试与 CI 入口、已有 spec/plan 归档，并记录 `AGENTS.md` 中的贡献约束。
- 确认现有 `docs/` schema/reference 文档由 `tfplugindocs` 生成，23 个资源和 19 个数据源均有示例来源。
- 为 QEMU VM `protection` typed 字段新增预期失败的 client、mapping、raw conflict 与 schema 属性单元测试。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU `protection` typed boolean，覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；`raw.extra_config["protection"]` 迁移到 typed 字段。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU `scsihw` typed 字段，覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；`raw.extra_config["scsihw"]` 迁移到 typed 字段。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU `tablet` typed 字段，覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；`raw.extra_config["tablet"]` 迁移到 typed 字段。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU `vga` typed 嵌套块（`type`/`memory`/`clipboard`），覆盖 schema、parse/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；无法解析的 `vga` 语法仍回退到 `raw.extra_config["vga"]`。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU `serial` typed slot map（`serial0`–`serial3`），覆盖 schema、client slot 分类、state/request mapping、per-slot raw 冲突校验、测试、示例和生成文档；`raw.extra_config["serialN"]` 迁移到 typed 字段。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU CPU 字段组（`numa` boolean、`vcpus`/`cpuunits` int64、`cpulimit` float64），覆盖 schema、新增 `proxmoxOptionalFloat64` 类型与 `setOptionalFloat64` helper、client decode/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；相关 `raw.extra_config` key 迁移到 typed 字段。
- 为 `proxmox_qemu_vm` 资源和数据源新增 Proxmox QEMU 内存 balloon 字段（`balloon`/`shares` int64、`hugepages` string enum），覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试、示例和生成文档；相关 `raw.extra_config` key 迁移到 typed 字段。
- 为 `proxmox_lxc_container` 资源和数据源新增 LXC CPU 字段（`cpulimit` float64、`cpuunits` int64），覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试和生成文档；相关 `raw.extra_config` key 迁移到 typed 字段。
- 为 `proxmox_lxc_container` 资源和数据源新增 LXC 控制台与脚本字段（`console` boolean、`tty` int64、`cmode`/`hookscript` string），覆盖 schema、client decode/encode、state/request mapping、raw 冲突校验、测试和生成文档；相关 `raw.extra_config` key 迁移到 typed 字段。
- 为 `proxmox_lxc_container` 资源新增 LXC clone 创建路径（`clone` create-time 嵌套块：`source_node`/`source_vmid`/`full`/`snapshot_name`/`storage`/`bwlimit`），覆盖 client `CloneLXCContainer`（`POST /nodes/{node}/lxc/{vmid}/clone` + task wait）、create 路径分支（clone vs ostemplate）、state 映射、测试、示例和生成文档；clone provenance 不可从 Proxmox 反查，导入/无 prior state 的 refresh 读回为 null。
- 新增 `proxmox_lxc_snapshot` 资源，覆盖 LXC 容器快照 CRUD（`POST/GET/PUT/DELETE /nodes/{node}/lxc/{vmid}/snapshot[/{snapname}]` + task wait）、schema（`node`/`vm_id`/`name` required、`description`、只读 `parent`/`snaptime`）、import（`node/vm_id/name`）、provider 注册、测试、示例和 reference 文档。
- 新增 `proxmox_qemu_snapshot` 资源，覆盖 QEMU VM 快照 CRUD（`POST/GET/PUT/DELETE /nodes/{node}/qemu/{vmid}/snapshot[/{snapname}]` + task wait）；提取共享 `client_snapshot.go`（QEMU/LXC 通用 snapshot 操作），schema、import、provider 注册、测试、示例和 reference 文档与 LXC snapshot 对齐。
- 将 `proxmox_lxc_container` 资源和数据源的 `network`（`netN`）与 `mount_point`（`mpN`）从 `map[string]string` 升级为 typed 嵌套块（类比 QEMU network/disk 的 parse/encode + raw 回退），覆盖 network 的 `name`/`bridge`/`ip`/`gw`/`ip6`/`gw6`/`hwaddr`/`type`/`tag`/`trunks`/`rate`/`mtu`/`firewall`/`link_down` 与 mount_point 的 `volume`/`mp`/`size`/`backup`/`ro`/`quota`/`replicate`/`shared`/`acl`；无法解析的语法仍回退到 `raw.extra_config`，保持单一 source of truth。
- 新增 `proxmox_storage` 资源，覆盖 Proxmox VE 存储池管理（`POST/GET/PUT/DELETE /storage`），typed 常用字段（`storage`/`type` required + `content`/`nodes`/`disable`/`shared`/`path`/`pool`/`vg_name`/`thin_pool`/`server`/`export`/`share`/`username`/`password`/`monhost`/`datastore`/`namespace`/`fingerprint`/`smb_version`/`options`/`format`/`mkdir`/`sparse`/`nocow`/`krbd`/`blocksize`/`fs_name` + `raw.extra_config` 回退）、`storage`/`type` RequiresReplace、delete-key diff、import（`storage`）、provider 注册、测试、示例和 reference 文档。
- 新增 `proxmox_role` 资源（`/access/roles` CRUD，`role_id` required + `privs` 权限字符串）和 `proxmox_user` 资源（`/access/users` CRUD，`user_id` required + `comment`/`email`/`enable`/`expire`/`firstname`/`lastname`/`groups`/`keys`/`password`），覆盖 client、schema、import、provider 注册、测试、示例和 reference 文档；补全 access 管理 RBAC 三件套（group/role/user）。
- 新增 `proxmox_acl` 资源，覆盖 Proxmox VE 权限绑定（`GET/PUT /access/acl`），管理给定 `path` 下的 role×user/group 绑定（`path` required + `roles`/`users`/`groups` 列表 + `propagate`），支持 update diff（移除已删除绑定）、import（`path`）、provider 注册、测试、示例和 reference 文档；配合 group/role/user 形成完整 RBAC 工作流。
- 新增 `proxmox_storage` 和 `proxmox_storages` 数据源，覆盖存储池查询（`GET /storage/{storage}` 单个、`GET /storage` 列表），provider 注册、示例和 reference 文档。
- 新增 `proxmox_user` 和 `proxmox_users` 数据源，覆盖用户查询（`GET /access/users/{userid}` 单个、`GET /access/users` 列表），补全 access 管理数据源；provider 注册、示例和 reference 文档。
- 新增 `proxmox_role` 和 `proxmox_roles` 数据源，覆盖角色查询（`GET /access/roles/{roleid}` 单个、`GET /access/roles` 列表），provider 注册、示例和 reference 文档。
- 新增 `proxmox_node_firewall_options` 资源，覆盖节点防火墙选项管理（`GET/PUT /nodes/{node}/firewall/options`），typed 全部 20 个选项（`enable`/log_level_in/out/forward/conntrack/synflood/smurf/tcpflags/nftables 等），delete 通过 reset 所有 key 实现，import（`node`）、provider 注册、测试、示例和 reference 文档。
- 新增 `proxmox_user_token` 资源，覆盖 API token 管理（`POST/GET/PUT/DELETE /access/users/{userid}/token/{tokenid}`），`user_id`/`token_id` required + `comment`/`expire`/`privsep`，create 返回的敏感 `value`/`full_token_id` 保存在 state（不可从 Proxmox 读回），import（`userid/tokenid`）、provider 注册、测试、示例和 reference 文档。
- 新增并扩展 `proxmox_firewall_rule` 资源，覆盖 cluster、node、QEMU/LXC guest 和 security group `/rules` CRUD；使用 content-based identity、computed `pos` 并在每次 mutation 前重新读取 ruleset 定位，duplicate pre-check（≥1 match 报错）、ambiguous match（≥2 报错）、可用时携带共享 digest，并通过 private state 仅删除 Terraform 曾托管的可选字段；不支持不稳定的 positional import。
- 新增 `proxmox_guest_firewall_options` 资源，覆盖 VM/容器级防火墙选项（`GET/PUT /nodes/{node}/{qemu|lxc}/{vmid}/firewall/options`），typed 10 个选项（`enable`/`dhcp`/`ipfilter`/`macfilter`/`log_level_in/out`/`policy_in/out`/`ndp`/`radv`），`node`/`vm_id`/`guest_type`（qemu/lxc）RequiresReplace、delete 通过 reset 所有 key 实现、import（`node/vm_id/guest_type`）、provider 注册、示例和 reference 文档。
- 新增 `proxmox_lxc_container` 资源和数据源支持，覆盖 LXC client、task wait、schema、mapping、raw 冲突校验、provider 注册、示例和 reference 文档。
- 修复 Tests CI：清理 `golangci-lint v2.12.2` 报告的 17 个 lint 问题，并重新生成 18 份未同步的 Provider reference 文档。
- 对照当前 Provider 注册、Proxmox VE API Viewer 和成熟 Provider 的覆盖面完成后续功能研究；修正 README 与代码库文档中过时的 3 resources/12 data sources 清单，并建立持续同步实际注册项的维护约束。
- 新增 `proxmox_cluster_firewall_options` 资源，覆盖 `GET/PUT /cluster/firewall/options`，管理 cluster-wide `enable`、`ebtables`、默认 in/out/forward policy 和 `log_ratelimit`；Terraform delete 通过 `delete` reset 全部托管 key，支持固定 ID `cluster` 导入，并补齐 client/resource 测试、示例和生成文档。
- 新增 `proxmox_backup_job` 资源，覆盖 `/cluster/backup[/{id}]` 同步 CRUD；支持固定 job ID、schedule/storage/node、all/pool/vmid 互斥选择、exclude、mode/compression、bandwidth、notification、notes、protected、repeat-missed 和 `prune-backups` retention，更新通过 `delete` 清理移除字段，删除仅移除计划且不触发备份任务，并补齐校验、测试、示例和生成文档。
- 新增 `proxmox_storage_file_download` 资源，覆盖 `/nodes/{node}/storage/{storage}/download-url` 和 content item GET/DELETE，支持 `iso`/`vztmpl`/`import`、checksum、解压和 TLS 校验；将 LXC 专用 task waiter 提取为通用 node UPID waiter，create/delete 均校验最终 exit status，并修正 API path 的 escaped-segment 处理以安全支持包含 `/` 的 volume ID，补齐校验、测试、示例和生成文档。
- 新增 `proxmox_cluster_metrics_server` 资源，覆盖 `/cluster/metrics/server/{id}` 同步 CRUD，支持 Graphite、InfluxDB 和 Proxmox VE 9 OpenTelemetry 字段、digest guarded update、字段移除、import 与 write-only InfluxDB token 保留；复用并扩展现有 metrics client，同时保持 list data source 兼容，补齐测试、示例和生成文档。
- 补齐 cluster firewall named objects：新增 `proxmox_cluster_firewall_alias`、`proxmox_cluster_firewall_ip_set`、`proxmox_cluster_firewall_ip_set_entry` 和 `proxmox_cluster_firewall_security_group`，支持 stable-name/CIDR identity、digest guarded update、import 和非 force IP set 删除；将 `proxmox_firewall_rule` 扩展到 cluster/node/QEMU/LXC/security-group scope，并确保 positional rule 在每次 create/update/delete 前重新读取当前 ruleset，补齐 exact-form HTTP 测试、scope/ownership 测试、示例和生成文档。
- 新增 `proxmox_replication_job` 资源，覆盖 `/cluster/replication[/{id}]` 配置 CRUD；使用 stable `<guest>-<job-number>` identity、immutable target、digest guarded update、private managed-field deletion 和 import，`source`/guest/job number/type 仅作为 observed computed state；不调用 replication run-now，destroy 使用 `force=1` 只移除 job config，不隐式清理 replication snapshots 或 target data，并以 exact-form HTTP 测试覆盖单节点 e2e 无法验证的多节点行为。
- 新增 `docs/guides/provider-configuration.md`，补充 endpoint/TLS、API token 与 ticket 认证组合、环境变量优先级、权限规划、state 安全和常见 API 错误排障；扩展 `tools/ci/README.md`，记录本地依赖安装、KVM/TCG 边界、只读 smoke test、后台 QEMU 停止/重建、日志诊断和 CI cache 与本地复现差异，并从 README 和代码库文档建立入口。
- 新增面向 Proxmox VE 9 的 `proxmox_realm` 资源，覆盖 `/access/domains[/{realm}]` LDAP、AD 和 OpenID Connect 外部 realm CRUD；使用单一 typed-only variant schema，拒绝内建 `pam`/`pve`，支持 digest guarded update、private managed-field deletion 和 import；LDAP bind password 与 OpenID client key 使用 Terraform 1.11+ WriteOnly + version 轮换，API 读回的 client key/password/TFA 不进入 state，并补齐 PVE 9 `audiences`、exact-form HTTP、variant/secret 测试、示例和生成文档。将手工指南移入 `templates/guides/`，确保 `make generate` 不再删除。
- 将 GitHub Actions 单节点 e2e 环境从 Proxmox VE 8.4-1 升级到 9.2-1，切换到 Debian 13 Trixie 的 `proxmox-auto-install-assistant` 9.2.7，并重新固定 ISO/assistant SHA256；e2e host 固定 Ubuntu 24.04 以满足 assistant 依赖，answer file 迁移到 kebab-case keys，cache key 随新 pins 自动失效旧 qcow2，contract test 固定 PVE 9/Trixie 输入，acceptance smoke 明确要求 API release major 为 9。
- 从当前仓库移除 `pve-docs` submodule 与 `.gitmodules`，将本地 `pve-docs/` 和 `.omx/` 加入 ignore，清理 README/copywrite 的 bundled mirror 配置，并删除已跟踪的 `.omx` 配置文件；仅提交当前树变更，不改写 Git 历史。
- 对照当前 PVE 9.2 API Viewer、官方 HA source 和 Provider 注册/生命周期模式完成下一项功能研究；推荐先补单 realm 只读 `proxmox_realm` data source，允许读取内建 `pam`/`pve` 但保持 resource 不可管理，复用现有 GET 和 secret 过滤边界，并以 PVE 9.2 `pam` realm 扩展只读 smoke。HA resource + affinity rule 作为下一项中型设计；QEMU compound devices 先阻塞于 removed-slot delete 基础，node networking/SDN 继续要求显式 reload/apply 边界。
- 新增单 realm 只读 `proxmox_realm` data source，复用 `GET /access/domains/{realm}` 并输出 LDAP、AD、OpenID Connect 与内建 realm 的公开 typed 字段；允许查询 `pam`/`pve`，但不暴露 password、client key、certkey、TFA、digest 或 resource secret version，不调用 `/sync`。补齐 exact GET、LDAP/AD/OpenID/内建映射、API error context 和 secret exclusion 测试、示例、生成文档，并将 PVE 9.2 smoke 扩展到读取 `pam`。
- 完成 PVE 9.2 `proxmox_ha_resource` 交付研究：下一项先独立管理现有 `vm:<vmid>`/`ct:<vmid>` 的 HA enrollment、显式 requested state、failback、per-resource auto-rebalance、restart/relocate limits 与 comment；读取 collection 规避 missing item 返回非 404，更新使用 fresh shared digest，destroy 固定 `purge=0` 且绝不删除/停止 guest。Terraform schema 不暴露过渡期 legacy group 或 `enabled` alias，不调用 migrate/relocate/CRM/arm-disarm，也不等待运行时放置收敛；typed affinity rule 后续单独交付。
- 新增 PVE 9.2 `proxmox_ha_resource` 资源，按研究契约实现 canonical `vm:<vmid>`/`ct:<vmid>`、显式 requested `state`、`comment`、`failback`、`auto_rebalance`、`max_restart` 和 `max_relocate`；通过 collection GET 识别远端缺失并取得 fresh shared digest，使用 private managed fields 删除 Terraform 曾管理的可选字段，支持 import。Destroy 在删除前重读 collection 并固定发送 `purge=0`，只移除 HA 配置，不调用 guest 删除/停止、migrate/relocate、CRM 或 arm/disarm endpoint；补齐 exact-form HTTP、PVE effective defaults、validation/ownership/error context 测试、示例和生成文档。
- 完成 v0.1.0 发布准备：整理正式 changelog 与迁移说明、同步 README 的 Go 1.26.4 构建要求、让 release workflow 按 tag 提取当前 changelog 段作为 GitHub Release notes，并移除未使用的 `main.commit` linker 注入。
- 扩展单节点 PVE 9.2 e2e 覆盖：只读测试新增 node status/DNS/time、cluster resources/metrics、storage、pool、group、role 和 user 数据源；隔离 CRUD 测试使用随机 ID 覆盖 pool、group、role、user、API token 与 ACL 的 create/update/read/delete，并在测试结束时销毁对象。真实 PVE 9.2 本地验证将 e2e statement coverage 从 5.6% 提升到 20.1%，同时修复 PVE 9 user groups 数组解码、role privilege map 解码、pool computed members unknown state，以及 pool/group DELETE 请求契约。CI 继续使用精确 `-run` selector，避免误执行其他 acceptance tests。
- 将 `internal/provider` 完整 Go statement coverage 从 46.6%（2,787/5,983）提升到 83.6%（5,019/6,002）。新增 schema-backed Terraform Plugin Framework 生命周期测试和本地 mock HTTP 合约，覆盖全部数据源以及 access、pool、firewall、backup、replication、metrics、HA、storage、snapshot、QEMU/LXC 等资源家族，不依赖 Terraform CLI 或真实 PVE；同时修复 guest firewall import 的 VMID 类型转换和 QEMU create/clone/delete task completion 等待。按要求仅报告覆盖率，不增加 CI threshold gate。
- 将单节点 PVE 9.2 e2e 扩展为三个精确选择的 acceptance 测试：新增随机高 VMID 的空 QEMU source VM 与同节点 full clone，通过 Terraform Plugin Testing 验证 create/clone/delete UPID completion polling、稳定 state 字段和正常 destroy；PreCheck 使用相同环境创建真实 client、发现节点并拒绝复用已有 VMID，失败清理按 clone 后 source 顺序执行、忽略 404、核对随机 owned name 后才删除并验证缺失。该测试不添加 disk/storage/network，也不启动 VM。首次真实运行确认 PVE 9.2 对缺失 QEMU config 返回 HTTP 500，client 现将该精确 node/VMID 响应归类为 not found 并保留底层 API error 链；修复后的 [Tests workflow 29891293810](https://github.com/Indexyz/terraform-provider-proxmox/actions/runs/29891293810) 全绿，三个真实 PVE 测试全部通过，QEMU task-waiting 场景完成 create/clone/read/destroy 与缺失校验，e2e statement coverage 从 20.1% 提升到 25.9%。
- 为 `proxmox_qemu_vm` 新增基于 `GET /cluster/nextid` 的 VMID 自动分配：`vm_id` 改为 Optional + Computed（`UseStateForUnknown` + `RequiresReplace`），省略时在 create/clone 任务启动前分配下一个空闲集群 VMID；新增 Optional `vm_id_start`（RequiresReplace）作为分配下限，因该 endpoint 不支持扫描起点，通过 propose-assert 循环断言候选 ID 空闲（被占用返回 HTTP 400 `already exists` 则递增重试），并与 `vm_id` 互斥（任一为 unknown 时延迟判定）、限定 100–999999999 范围；client 新增容忍 string/number 响应解码的 `GetNextVMID`；分配结果写入 state，`vm_id_start` 通过 prior model echo 保留；create/clone 任务成功后立即将 id/node/vm_id/vm_id_start 写入 response state（persistQemuVMIdentity），避免后续 clone post-update 或 read 失败时已创建 guest 脱离 Terraform 跟踪、下次 apply 重新分配造成重复 VM；data source 按既有 clone provenance 模式输出 computed null `vm_id_start`。补齐 client/query/400 透传、自动分配与 start 循环生命周期、conflict/range ValidateConfig 测试、示例与生成文档；契约研究见 [`research/proxmox-nextid-allocation.md`](../research/proxmox-nextid-allocation.md)。
- 解码层简化：合并 5 个 wire/public 孪生结构（`nodeFirewallOptionsKnown`、`guestFirewallOptionsKnown`、`qemuVMConfigKnown`、`lxcContainerConfigKnown`、`storageConfigKnown`）到对应公开结构体（json tag 直接标注、非 wire map 字段 `json:"-"`）；node/guest 防火墙选项 Get 改为直接 unmarshal（对齐 `ClusterFirewallOptions` 既有模式），QEMU/LXC/storage 保留 raw map 与 ExtraConfig 未知键分类、删除逐字段拷贝块；删除 `waitSnapshotTask` 纯转发中继（调用方改用 `waitForNodeTask`）；`setOptionalBool` 复用 `boolToFormValue`。经 AST 探针验证三组 tag 与 knownKeys 排除集一一对应（33/23/28），行为等价；全套件 234 PASS，gofmt/vet/deadcode 干净，净减约 250 行。
- 完成 v0.2.0 发布准备：整理 QEMU VMID 自动分配、PVE 9 API 兼容修复与任务等待修复的正式 changelog，发布标签继续由 GoReleaser workflow 构建并签名。
- 新增 `proxmox_qemu_vms` 列表数据源：基于 `GET /cluster/resources?type=vm`（该 endpoint 无服务端 qemu/lxc 过滤，客户端按 `type` 字段剔除 LXC），提供 `template`/`name`/`node` 客户端精确过滤（`template` 字段缺失按 API 默认 `0` 视为非模板参与过滤、输出保留 null），`vms` 输出按 `vm_id` 数值升序排序（规避服务端 vmid 字符串字典序），空结果为合法空列表（唯一性断言交给 Terraform `one()`）；同时修复 `ClusterResource.Template`/`Shared` 因 PVE 数字 `0|1` 序列化导致 `*bool` 解码失败的潜在缺陷（改用 `proxmoxOptionalBool`，该缺陷此前未暴露是因为 e2e smoke 仅查询 `type=node`）；补齐 client 解码（数值/布尔容忍）、过滤矩阵、缺失 template 字段、`99`/`100` 排序陷阱、filter echo 等测试、示例与生成文档。研究契约以 git.proxmox.com pve-manager HEAD（`PVE/API2/Cluster.pm` + `PVE/API2Tools.pm`）钉扎，research 与实现均经 gpt-reviewer 审阅通过，见 [`research/proxmox-qemu-vms.md`](../research/proxmox-qemu-vms.md)。
- 新增 `proxmox_storages` 过滤与容量选择：数据源在 `GET /storage` 配置列表基础上，合并 `GET /cluster/resources?type=storage` 的每节点 `maxdisk`（按 storage 聚合取最大，共享存储各节点同容、节点本地存储取最大实例；未上报容量记为 null），新增 Optional `type`（插件类型精确匹配，空值禁用）与 `largest`（其余过滤后仅保留并列最大容量的记录，null 容量被排除、全部缺失时返回空列表、并列全部返回由 `one()` 断言唯一），记录新增 computed `capacity_bytes`；保持既有 6 字段与 API 顺序不变。研究文档经 gpt-reviewer 两轮审阅（修正生命周期 fixture 约束与“未索引节点条目仍在响应中”的表述），实现审阅 approve，见 [`research/proxmox-storages-filters.md`](../research/proxmox-storages-filters.md)。
- 为 `proxmox_qemu_vm` 新增 VM 生命周期钩子与失败归属（input.md 计划 Stage A）：新增 Optional `start_on_create`（create/clone 任务与 post-clone `/config` 更新成功后调用 `POST /nodes/{node}/qemu/{vmid}/status/start` 并等待任务完成；属 create-time hook 而非声明式电源状态，更新该选项或 refresh 已停止 guest 绝不触发 start/replace，失败时已创建 guest 连同 stop 策略保留在 state 中可重试）与 Optional `stop_on_destroy`（destroy 先以 config GET 安全发现缺失 guest，含 PVE 对缺失 QEMU config 的精确 HTTP 500 归类，仅在 status 为 running 时 `status/stop` 并等待任务成功后再删除；停止失败、超时或取消一律中止删除保持 guest 可重试，已停止/缺失 guest 直接删除，重复 destroy 幂等成功）；client 新增 `StartQemuVM`/`StopQemuVM`（异步 endpoint 必须返回 UPID，空 ack 报错而非假成功）；`persistQemuVMIdentity` 同步持久化两个生命周期策略；Update 仅持久化策略变更不触碰运行时；数据源对两个 Terraform 侧策略输出 computed null（与 `vm_id_start` 模式一致）。补齐 client start/stop 精确 HTTP 与空 UPID 契约、plain-create/clone 启动顺序、默认与 read/update 无动作、启动失败保留身份与策略、stop 成功/失败/超时、缺失/已停止/重复 destroy、中断删除重试、create/clone POST 撞上已占用 VMID 的候选竞争（返回原始错误且绝不采纳、停止、更新或删除获胜 VM）等测试，并重新生成文档。
- 新增 `proxmox_nocloud_iso` 资源（input.md 计划 Stage B）：内置基于 `github.com/kdomanski/iso9660` 的纯 Go ISO9660 生成器，以 `CIDATA` 卷标将 `user-data`/`meta-data`/`network-config` 写入根目录（d1 字符 + `;1` 版本号，Linux isofs 挂载后还原为小写连字符文件名，cloud-init NoCloud 可读），staging 目录 0700 私有、全路径清理且清理错误并入返回错误，并以原始字节解析（PVD 卷标、根目录记录、extent 内容逐一比对）与 ISO reader 回读双路验证实际产物；新增 `POST /nodes/{node}/storage/{storage}/upload` multipart 上传 seam（复用既有 auth/TLS/envelope 处理，`content` 表单字段 + `filename` 文件部件，空 UPID 应答报错而非假成功），按 UPID 解析真实 task owner 节点轮询（该 endpoint 非 proxyto node，owner 可与目标节点不同）；新增 `GET /nodes/{node}/storage` 节点存储索引校验（目标 storage 可见、enabled、active、支持 `iso` content）与 `GET .../content?content=iso` 集合读取作为权威存在性判定（单个 item GET 的 404 不可靠），上传前拒绝已存在目标卷并在 schema 文档声明 PVE 无原子 create-if-absent、调用方需自行保证跨 state/代次唯一；资源 schema：`node`/`storage`/`filename`/`user_data`/`meta_data`/`network_config` required + RequiresReplace（三个内容输入 sensitive，state 明文警示），computed `volume_id`/`id`，filename 限定安全 `.iso` basename；生命周期：上传 POST 接受后立即持久化身份与 accepted upload UPID 到 private state 再等待任务，等待失败保留可重试状态（创建失败≠成功，但已接受的任务不脱轨），Read/Delete 先对账 retained upload/delete 任务（still-running 阻止缺席判定与清理、未知状态报错重试、已停止任务放行并从 private 清除），Read 保留 write-only 内容输入，删除仅针对 state 记录的精确卷、接受 DELETE UPID 后先记录再等待、失败可重试且缺失幂等；补齐 multipart/auth/escaping、storage 拒绝矩阵、existing-file 拒绝、异步成败/轮询失败、失败上传→refresh→清理恢复（无孤儿 ISO、无提前删除）、retained delete task 重试与幂等删除等测试、示例与生成文档。
- 完成 input.md 计划 Stage C：将 CD-ROM 附着安全接入 `proxmox_qemu_vm` 创建路径（计划内 `media = "cdrom"` 或 `.iso` 卷引用的 typed disk 槽位：CD-ROM 仅允许 ide/sata/scsi 槽位；计划内最多一个 seed 卷；seed storage 必须在 VM 所在节点可见、enabled、active 且支持 `iso` content，与 VM 磁盘 storage 解耦，`ensureISOStorage` 从 nocloud 资源方法重构为共享自由函数）；clone 分支在持久化克隆身份后、应用 `/config` 更新前读取原始 wire disk 配置（不依赖 typed 完整解析，含未知选项的 wire 值同样识别）：目标槽位仅允许已持同一卷、空 `none` 盘位、或 PVE 为该 VM 生成的 `vm-<vmid>-cloudinit` cloud-init 盘显式同槽替换，覆盖硬盘/外来 ISO/他 VM cloud-init 盘一律拒绝，且附着后不得残留第二个 ISO/cloud-init 盘（双 seed 拒绝，物理 `cdrom`/空盘位不计）；失败发生在 start 之前且克隆身份已持久化可重试；plain-create 在任何 HTTP 之前拒绝不安全计划。修复 Stage A review 发现：Delete 不再吞掉 `StopQemuVM` 的 `errNotFound`（stop 任务轮询 404 不证明 VM 已消失或停止成功），所有 stop 错误一律中止删除并补充任务状态 404 回归测试证明无 VM DELETE，后续重试由 config GET 判定真实缺失；`stop_on_destroy` schema 文档明确硬断电语义（`qm stop`，非优雅关机）。新增指南 `templates/guides/nocloud-runner-vm.md.tmpl`（token-only 模板查找 → `one()` 唯一匹配 + 非空 precondition → 每代次专属 ISO（自带 DHCP network-config）→ `vm_id_start` clone → CD-ROM 引用 → `start_on_create` + `stop_on_destroy`；Terraform 依赖/销毁顺序与代次更替：ISO 资源 `create_before_destroy`、`volume_id` 引用排序 VM 先于 ISO 删除、generation 驱动 `vm_id_start` 触发 VM 替换；templatefile()/yamlencode() 渲染边界、sensitive/state/远端 ISO 明文警示、ISO storage/节点约束、并发命名与 nextid 语义、支持的模板布局、失败恢复、以及真实 PVE/Shaula 手工验收清单并显式标注 guest DHCP/cloud-init/Runner job/多节点行为未被 API mock 验证）；补全链路 mock 集成测试（seed 上传 → clone → 同槽替换 → start → 硬断电销毁 → ISO 清理的严格调用序）与外来介质/双 seed/硬盘覆盖/virtio 槽位/双计划 seed/节点存储不可用拒绝矩阵、plain-create CD-ROM 表单契约、wire helper 与 ownership 单元表；同步 qemu_vm 示例（生命周期钩子 + ide2 CD-ROM）、README、codebase 文档与 `make generate` 产物。
- Review round 1 修复切片 2/5/6/7（任务所有权边界）：保留任务（retained task）全部按 UPID 内嵌 owner 节点轮询（NoCloud ISO 的 Read/Delete 对 retained upload/delete 任务先以 `taskOwnerNode` 解析 owner 再轮询/等待，资源节点不再被误当作 task 节点；DELETE 新接受任务同样先验证 ack 的 owner 再等待，跨节点 owner≠上传节点链路（接受超时 → 刷新期间 running → 停止后清理）有回归测试保证不请求错误节点、不提前清理、不产生孤儿介质）；QEMU create/clone 拆分接受与等待（新增 `SubmitCreateQemuVM`/`SubmitCloneQemuVM` 返回已接受 UPID，awaited 便捷方法改为 submit+wait 组合；资源在 POST 接受并验证 ack 后、等待前持久化身份/策略与 private pending UPID，轮询超时/取消/任务失败仍报错但 guest 与 pending 任务保留，Read 先单次轮询 owner 节点对账 pending 任务——running 保持 state 不判缺席、已停止清除后回普通读/删流程，Delete 先等待 pending 任务再清理，POST 被拒（候选 VMID 竞争）绝不入 state）；delete-task 404 不再吞掉（`QemuVMResource.Delete` 移除对 `DeleteQemuVM` 返回 `errNotFound` 的吞啦，幂等缺席判定只保留在事前 config GET 与 DELETE 提交响应，任务轮询 404 保留错误与 state 可重试，重复 destroy 对真实缺失仍幂等成功）；任务边界应答强化（新增 `validateQemuTaskAck`：空/垃圾/无 owner 的 UPID ack 一律报错且不轮询，覆盖 start/stop 与 create/clone 提交；新增 `nodeTaskFinished` 仅认 running/stopped，未知或缺失状态直接报错而非永久静默等待，`waitForNodeTask` 与 NoCloud retained `taskFinished` 共用该分类，未知状态重试可恢复）。
- Review round 1 修复切片 1/3/4/8 + owner 批准的 `nocloud_cdrom_slot` 标记：新增 Optional `nocloud_cdrom_slot`（create-time 输入，变更 RequiresReplace，prior state echo、数据源 computed null、persistQemuVMIdentity 一并持久化；ValidateConfig 校验槽位语法，未标记工作流保留普通多 ISO/native cloudinit 能力，不再受单 seed 限制）。修复 1（marked seed 在 VM 节点真实卷可见性）：标记工作流先于 clone 以 VM 节点 `GET .../content?content=iso` 权威集合读取确认精确 volume_id 存在（同名校名非共享存储在其他节点缺文件先失败；403/500 保持错误不判缺席；`storageFileExists` 收敛为共享 `isoVolumeExists` 自由函数）；跨节点正 fixture 建模共享存储并在目标节点暴露精确卷。修复 3（伪介质覆盖防护）：标记工作流对每个计划 CD-ROM 变更（含 `none`/`cdrom`/未设）检查继承目标所有权——伪值不得清除继承的硬盘/外来介质，标记槽位必须计划真实 ISO 卷，seed 计数检查完整有效 wire 配置（typed parser 未识别的 raw 盘同样计入第二个 seed）；未标记工作流保持原行为（仅守卫真实卷，不计数）。修复 4（继承 PVE 文件型 cloudinit 语法）：`qemuVMIsPVECloudInitDrive` 按 PVE::QemuServer::Drive::drive_is_cloudinit 语法识别 `storage:vm-<id>-cloudinit` 与文件型 `storage:<id>/vm-<id>-cloudinit.<raw|qcow|qcow2|qed|vmdk|cloop>`（要求 `media=cdrom` 与精确 owner VMID，后缀锚定不采纳外来/仿似文件），LVM 与文件型同槽替换、他 VM/普通盘/未知后缀拒绝均有测试；标记化 update 额外允许替换 prior state 中本资源自身附着的旧 seed（seed 再生成路径）。标记化 in-place update 在 PUT 前完整校验附着安全（标记槽位真实卷、存储可用且精确卷存在、wire 所有权/计数守卫），不安全变更先拒绝且不发起 PUT、不 start；Read/update 从不触发动作。修复 8（跨节点 clone wire 契约）：`SubmitCloneQemuVM` 目的地改用官方 `target` 表单键（不再发送路由参数 `node`），URL 路径与任务轮询仍为源节点，client 级 exact-form 测试覆盖跨节点/同节点 `target` 并断言无 `node` 键，LXC clone 逻辑不动，`clone.source_node` 文档声明跨节点需共享存储限制。
- Review round 1 修复切片 9（指南代次/恢复/安全描述准确性 + Terraform core 代次更替验证）：指南示例改为常量 `vm_id_start = 100` + `nocloud_cdrom_slot = "ide2"` 标记 + VM `lifecycle.replace_triggered_by = [proxmox_nocloud_iso.runner_seed]` 驱动代次替换，并如实说明替换可能复用刚释放的数字 VMID（不承诺新 ID、VMID 非身份）；CD-ROM 安全章节改为标记限定语义（未标记普通多 ISO/native cloudinit 不受限制），补充同槽 PVE cloudinit 文件型替换、伪介质防护与 raw 第二 seed 拒绝的描述；失败恢复如实改写：`nextid` 在 Create（apply）而非 plan 查询、propose-assert 跳过已占用候选、仅提交窗口竞争才报错且不触碰获胜 VM；Create 失败后实例可能被 taint，后续 apply 是清理替换而非同 guest 修复/重启；上传 UPID 前的传输失败不可区分，需人工检查存储而非“绝不孤儿”承诺；ISO 生成描述改为“明文写入私有 0700 临时 staging 目录 → 内存组装”，含崩溃残留与 TMPDIR/state 保护警示；新增 TF_LOG=DEBUG/state/user-data/任务日志先脱敏再发布的要求；验收清单第 2/12 步按上述真实语义改写。新增 `terraform_core_generation_test.go`：构建 provider 二进制并以真实 Terraform CLI（dev_overrides）+ 本地 mock PVE API 执行 generation 1 apply → generation 2 替换 → destroy，断言 core 依赖图导出的顺序（seed 上传先于 clone、替换时旧 VM 先销毁后新建、旧 seed 仅在替换 VM 重新指向并启动后才删除、销毁时 stop + VM 删除先于 seed 删除），并断言常量 `vm_id_start = 100` 分配与释放后复用；README 补注册 `proxmox_nocloud_iso`（26 个资源）并修正 in-memory 表述，codebase 文档计数同步为 26 资源/21 数据源，`make generate` 重复执行零 diff。
- Review round 2 修复切片（parent 独立 overlay 探针证明的三处缺陷）：（1）标记工作流的计划期 raw 盘安全：新增 `plannedQemuVMRawWireCDROMs`/`plannedQemuVMWireCDROMs`，将计划内 `raw.extra_config` 盘槽（`.iso` 卷或 `media=cdrom`，归一为裸卷引用）并入有效计划附着集，create/clone/update 的槽位适配、seed 计数与目标所有权检查均改用该完整集合——typed 标记 seed + raw ide3 第二 seed 在任何 POST/PUT 之前被拒，raw 计划伪值清除继承硬盘同样拒绝，marker 本身仍必须指向 typed 盘槽，未标记工作流保持 typed-only 集合、raw 透传不加检查、不重构通用 raw 映射；（2）未标记 clone 恢复基线普通 ISO 更换行为：`validateQemuVMCDROMOwnership` 完全限定于 marker 工作流（unmarked 直接放行，clone 不再做中间 config 读），继承 installer-old.iso → 计划 installer-new.iso 的普通 clone 替换成功且调用序与基线一致，标记工作流的外来 ISO/硬盘覆盖拒绝保留（相关拒绝测试改为标记模型并新增未标记成功替换测试）；（3）已终结失败任务放行直接销毁：Delete 对 retained create/clone 任务先以一次 owner 节点轮询分类，已停止（成功或失败）即清除 pending 并交由权威 guest 查找决定剩余清理（无需先 Read，guest 缺席则幂等成功），仍运行则等待，轮询错误/未知状态中止，等待期间任务失败仍阻止过早删除、重试按已终结任务放行。以上各配资源级回归：预修复 overlay 运行中 6 个回归全部失败、修复后全部通过。
- Review round 2 后继修复切片 B（真实模板根盘 vs Terraform Core 盘映射一致性 + 指南/预检准确性）：新增资源态盘映射投影边界 `projectQemuVMDiskState`（`qemuVMStateFromAPI` 内）：资源盘状态以 prior plan/state 已知盘键集合为准——计划只声明 ide2 时，clone 物理继承的模板系统盘（如 `scsi0`）不再作为新托管盘映射条目进入 Create/Update/Read 后的状态（消除真实 Terraform core 的 apply 后一致性错误 `new element has appeared`）；托管槽位始终携带实际观测 wire 值（绝不回显请求值，带外换盘保持可见 drift），wire 中消失的托管槽位被投影出状态；typed parser 无法解析的盘槽继续流入 `raw.extra_config` 保持可观测；prior 盘映射缺失/unknown（data source、import、未配置 computed 路径）时保留完整观测盘清单，prior 已知空映射（`disk = {}`）是受约束的空键集合，始终保留为已知空映射（绝不坍缩为 null 或扩为完整清单，包括观测清单缺失时）。Update 后读取改用当前计划的目标盘键集合（新增托管槽位在 PUT 后保留在状态中），其余 prior 语义（write-only 输入、marker、生命周期策略）不变。所有附着安全检查（计划期存储/卷存在性、clone 后 wire 所有权、update 前 wire 所有权/双 seed 计数）继续在盘状态投影之前检查完整 live wire 配置，继承/raw 第二 seed 不因非托管状态键而漏检。`terraform_core_generation_test.go` 的 mock 模板升级为真实可引导模板 clone（继承 `scsi0` 根盘 `local-lvm:vm-<id>-disk-0` + `net0` NIC + ide2 PVE cloudinit 盘，每次 config 读取都保留根盘/NIC，PUT 携带继承槽位键即失败），并以真实 Terraform CLI 完整验证 gen1 apply → 无操作重复 apply（零写请求）→ 带外更换托管 ide2 介质后 `plan -detailed-exitcode` 报告变更（托管槽位 drift 可观测）→ 恢复后重复 plan 干净（exit 0，computed uptime 不计为配置变更）→ gen2 替换 → destroy，根卷身份精确跟随 VMID 且 PUT 从不覆盖/移除根盘（不豁免根盘要求）。标记槽位预检按官方 PVE 语义（"explicit 'media=cdrom' is required for iso images"）要求标记槽位计划显式 `media = "cdrom"` 的真实 ISO CD-ROM：省略 media 或显式 disk 介质的标记槽位在任何 guest/clone 任务存在前拒绝，修正 `plannedQemuVMCDROMs` 中“.iso 隐式 cdrom”的错误注释（后缀仅作安全启发式），未标记普通行为不变。指南失败恢复章节改写：UPID 前传输失败不写任何 state、refresh 无法发现未跟踪文件、该资源无 import 可采纳——只能人工核对存储清单、证明专属所有权与任务静止后删除精确卷（绝不建议删除任意共享介质或经不支持的 import 采纳）；clone/create 提交失败区分 API 错误应答（从未接受）与响应前传输失败（结果未知，需人工核对候选 VMID/任务日志后再清理或经 `terraform import` 显式采纳）；CD-ROM 安全章节声明显式 media=cdrom 契约与拒绝行为。`proxmox_qemu_vm` 盘 schema 文档声明资源态键集合语义与 data source 全量清单入口。
- Review round 2 终审修复（refresh 不能把外来介质洗成自有附着）：移除 QEMU VM Update 中由刷新后 state 推导的 `ownedCDROMs` 与 `qemuVMIsOwnedCDROMMedium` 允许路径——supersedes round 1 修复切片 1/3/4/8 中“标记化 update 额外允许替换 prior state 中本资源自身附着的旧 seed”的历史行为：普通刷新观测到的介质现实不构成本资源的附着所有权，Read 继续如实记录实际 drift（绝不回显虚构旧值），也不引入替代性所有权注册表。标记化 in-place update 现在拒绝更换任何不同真实介质（含本资源先前 apply 附着过的旧 seed），仅保留同卷、空盘位与精确同 VM 的 PVE 生成 cloud-init 盘安全路径及全部既有守卫；拒绝错误明确指向以 VM 替换（如 lifecycle replace_triggered_by）完成 seed 更替，不提示任何采纳/覆盖路径。新增回归：真实 Read→Update 外来 ISO 序列（refresh 保留实际外来卷 drift、标记 update 拒绝且无 PUT/无 start）、原 owned-swap 正向测试改为旧 seed 就地更换拒绝、同 seed 仅生命周期策略变更的标记 update 仍成功且不重启；Update 中重复的标记槽位预检一并去重。schema `disk` 描述、指南 CD-ROM 安全章节与 codebase 文档同步改写并重新生成；rootdisk+NIC Core 代次/无操作/drift 测试保持不变。
- Review round 3 修复（已知空盘映射投影 + Update 增量写入 + 裸伪介质守卫）：`projectQemuVMDiskState` 不再把 prior 已知空盘映射（`disk = {}`）坍缩为 null 或扩为完整观测清单——它是受约束的空键集合，返回保留观测元素类型的已知空映射，且在观测清单缺失/unknown 时同样生效（消除真实 Terraform core 的 `.disk: was cty.MapValEmpty(...), but now null` 一致性错误）；prior null/unknown（data source、import、未配置 computed 路径）保持完整清单，托管槽位实际观测 drift 不变。映射层与真实 Terraform Core 根盘 harness 同步覆盖：harness 新增 `disk = {}` 的可引导模板 clone（vm_id 101，物理继承 `scsi0` 根盘与 `net0` NIC），验证 gen1 创建后状态保持已知空映射、`{} → {ide2} → {}` 两次 in-place 更新转换、转换后重复 plan 干净（exit 0），事件序断言改用精确 VMID 前缀。QEMU VM Update 写入收窄（parent 决策 A，仅 typed disk 与 network 两个映射）：Terraform 会用 prior 观测值填充未配置的 Optional+Computed 集合，最终 plan 会把继承的根盘/NIC 观测值当作新的变更意图，故 outgoing PUT 仅发送相对 prior state 新增或变化的槽位（未变化的继承观测值不再回发，带外 drift 不再被陈旧值覆盖）；新增/变化槽位保持发送，全部计划期/live typed+raw 安全检查在收窄之前对完整有效附着集合执行，create/clone 后初始附着不受影响，serial/ipconfig/标量/raw 不在本次范围。标记工作流的裸伪介质守卫补全（Standards round-3 P1）：位置式 `none`/`cdrom` 即使省略 `media=cdrom` 也作为安全相关的计划槽位变更纳入 typed 与 raw 两路所有权检查（不清除继承硬盘/外来介质、不计为 seed、未标记普通行为不变），资源级裸伪 clone/update 回归断言无 unsafe PUT/start。harness 加固：mock PUT 守卫先捕获越界表单再解锁后调用 `p.fail`（原先持锁自调用导致 mock 自死锁、整个 `go test` 挂至超时），守卫拒绝快速失败且表单内容进入诊断；mock PUT 现在应用 `name` 字段；terraform 子命令进度仅记录子命令。schema/文档重新生成可复现（与 round-3 checkpoint 字节一致），真实 guest 验收仍未验证。
- Review round 4 修复（无 delay 的异步 DELETE 应答不得成为假成功）：官方 `PVE/API2/Storage/Content.pm` 的内容 DELETE 仅在调用方显式提供 `delay` 参数（本 client 从不发送）时才可能返回 null，正常无 delay 路径返回 `imgdel` fork_worker UPID，因此 NoCloud ISO 删除的空/null 应答代表删除未经验证：`NoCloudISOResource.Delete` 移除空 UPID 静默成功路径，空/null/畸形 UPID 一律报错并保留 state 可重试（经既有 `taskOwnerNode` 拒绝且对空/垃圾应答零任务轮询），DELETE 提交 errNotFound（卷已消失）保持幂等成功、其余提交错误原样传播、有效 owner UPID 先记录后等待的行为不变；`Client.DeleteQemuVM` 在 DELETE 提交成功与等待之间补充既有 `validateQemuTaskAck` 校验（与 create/clone/start/stop 异步 endpoint 一致），空/畸形 `qmdestroy` 应答报错且不轮询，提交 missing-guest 幂等与 delete-task 404 报错路径保留，共享 `waitForNodeTask` 空容忍语义与 storage-download 资源不收紧、不新增 delay 支持或兼容回退。VM 资源级、NoCloud ISO 资源级与 QEMU client 级新增 HTTP200 `data=null`/空串/畸形非空 UPID 回归（断言保留 state/诊断且不发起任务轮询），既有有效 UPID 完成等待与真实缺失重复 destroy 测试保持绿色。

## 接下来

### 优先实现

input.md 计划的 NoCloud 交付链路（Stage A 生命周期钩子、Stage B NoCloud ISO 资源、Stage C CD-ROM 安全集成与指南）、Review round 1 九项必修（1–9）、Review round 2 三处缺陷修复与 Review round 4（无 delay 异步 DELETE 空/畸形 UPID 应答拒绝，覆盖 VM 与 NoCloud ISO 两路删除）已全部完成；真实模板根盘的 Terraform Core 盘映射一致性缺陷已在切片 B 修复（盘状态投影边界 + 真实 CLI 根盘 fixture 全链验证），refresh 所有权缺陷已按终审要求以拒绝路径修复（标记化 update 不再认可 prior-state 自有 seed 的就地更换，seed 更替走 VM 替换）。残留边界：未标记（无 `nocloud_cdrom_slot`）工作流（typed 与 raw 透传均）不做 CD-ROM 附着安全检查（严格检查按 owner 决策限定 marker 工作流；标记工作流的计划期 raw 盘槽自 round 2 修复起并入安全检查，不再是接受的绕过）。真实环境 DHCP/cloud-init/Runner 行为按指南验收清单人工确认，仍未验证。

下一项后续研究并设计单一 typed `proxmox_ha_rule`：覆盖 PVE 9 `node-affinity`/`resource-affinity`，继续使用 fresh shared digest、typed variant validation 与 import；rule 引用已经由 `proxmox_ha_resource` 管理的 canonical SID，不提供 legacy HA group 或 generic raw map。

### 后续中大型功能

- `proxmox_lxc_containers` 列表数据源：复用 `proxmox_qemu_vms` 的 client 查询与客户端过滤模式，面向 LXC 模板/容器搜索。
- Authentication realm 高级后续：按独立设计补充 list data source、LDAP group/sync 高级字段、client certificate 和 TFA；`/sync` 属命令式操作，不在普通 realm CRUD 中隐式执行。
- HA rules：在 `proxmox_ha_resource` 之后实现单一 typed variant resource，覆盖 PVE 9 `node-affinity`/`resource-affinity`、shared digest、feasibility error 与 import；不提供 legacy HA group 或 generic raw map。
- Node networking：覆盖 `/nodes/{node}/network`，将 pending 配置与 apply/reload 生命周期分离，避免单个接口资源自动 reload 导致中间状态或管理网络断连。
- SDN zones/VNets/subnets：作为独立 epic 处理 typed variants、共享 digest、依赖顺序和显式 activation，不在每个子资源 CRUD 后自动 apply。
- QEMU `hostpci`、`usb`、`rng`、`virtiofs` 等 compound device：按现有 slot parser/encoder 和 whole-slot raw fallback 模式实现；现有 `raw.extra_config` 可覆盖长尾，因此低于集群级能力。

### 持续约束

- 新增或修改 Provider schema 时运行 `make generate`，同步更新 `docs/index.md`、`docs/resources/`、`docs/data-sources/` 和示例。
- 当前 e2e 环境是单节点 Proxmox VE 9.2，运行三个精确选择的测试：扩展只读数据源、随机命名的 pool/access CRUD，以及无 disk/storage/network/runtime start 的空 QEMU source VM/同节点 full clone task polling。存储配置、防火墙、HA、replication、backup、外部 realm、LXC、QEMU/LXC 运行时操作、多节点或 ZFS 行为仍以 HTTP 单元测试覆盖，除非为其设计隔离的 acceptance 环境。
- QEMU/LXC 运行状态保持 observed-only；不要把 start/stop/rollback 等命令式操作伪装成普通资源的期望状态。
- 扩展 typed 字段时同步更新 schema、mapping、client 分类和测试，并保持 typed 与 `raw.extra_config` 单一 source of truth。
- 后续可按资源 family 对照固定版本 Proxmox API Viewer，补充经过验证的最小权限矩阵与 import 操作手册；在完成逐 endpoint 核验前不提供可能误导用户的通用权限角色。

研究依据：[Proxmox VE API Viewer](https://pve.proxmox.com/pve-docs/api-viewer/)、当前 `internal/provider/provider.go` 注册表、现有 client/resource 模式，以及 [BPG Provider 的公开资源覆盖面](https://github.com/bpg/terraform-provider-proxmox/tree/main/docs/resources)。候选比较见 [`research/proxmox-next-feature.md`](../research/proxmox-next-feature.md)；HA resource 的 PVE 9.2 schema、生命周期和验证边界见 [`research/proxmox-ha-resource.md`](../research/proxmox-ha-resource.md)。
